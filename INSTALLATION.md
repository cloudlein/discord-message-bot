run 
```bash 
    python -m venv venv
```

then run :
linux: 
```bash 
   source venv/bin/activate # linux 
```
windows:

```bash 
   venv\Scripts\activate
```

instal addtional package: 
```bash
    pip install -r requirements.txt
```